services:
  - type: web
    name: aethera-water-intelligence
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run .github/streamlit_app.py --server.address 0.0.0.0 --server.port $PORT
    healthCheckPath: /_stcore/health
    plan: free
